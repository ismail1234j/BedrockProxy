<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.BedrockRelay" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Add a dark mode theme -->
    <style name="Theme.BedrockRelay.Dark" parent="android:Theme.Material">
        <item name="android:colorBackground">@color/black</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>